# 연세척병원 웹 — 2026-07-11 작업 기록 (복구용)

> 파일 분실 사고 전, 이 세션에서 작업한 모든 변경 사항.
> GitHub(hoikjeon/yeonsei-cheok-web) 복원 후 이 문서를 보고 재적용할 것.
> ⚠️ 이 문서에 없는 것: 사용자가 직접 작업한 `HomeDoctorsRevealSection.tsx`(홈 의료진 리빌 섹션),
> about 페이지 VALUE_ITEMS 문구 교체(시술부터 수술까지! / 일대일 맞춤 치료 / 올바른 사후관리 / 원스톱 진료시스템, 아이콘 Stethoscope·UserCheck·HeartPulse·Zap)는 내용 일부만 파악됨.

---

## 1. `src/app/(public)/about/page.tsx` — 병원장 인사말 리뉴얼

- 스톡 사진 → 이남/김동한 병원장 누끼 2인 배치 (의료진 소개 히어로 스타일)
- 서명 변경: "연세척병원 병원장 드림" → **"연세척병원장 이남, 김동한 드림"**

데이터 구조:
```tsx
interface DirectorCutout {
  name: string;
  image: string;
  alt: string;
  figureClassName: string;
  labelClassName: string;
  imageClassName?: string;
}

interface IntroSection {
  title: string;
  image?: string;
  imageAlt?: string;
  directors?: DirectorCutout[];
  paragraphs: string[];
  signature?: string;
}
```

INTRO_SECTIONS[0] (병원장 인사말)의 directors:
```tsx
directors: [
  {
    name: '이남 병원장',
    image: '/generated/doctors-lineup/lee-nam.png',
    alt: '이남 병원장',
    figureClassName: 'left-[35%] top-[5%] z-20 h-[145%] w-[64%]',
    labelClassName: 'left-[35%]',
  },
  {
    name: '김동한 병원장',
    image: '/generated/doctors-lineup/kim-dong-han.png',
    alt: '김동한 병원장',
    figureClassName: 'left-[65%] top-[5%] z-10 h-[145%] w-[64%]',
    labelClassName: 'left-[65%]',
    imageClassName: 'scale-x-[-1]',   // 이 섹션에서만 좌우 반전
  },
],
signature: '연세척병원장 이남, 김동한 드림',
```

렌더링 블록 (기존 단일 Image 자리를 조건 분기로 교체):
```tsx
{section.directors ? (
  <div className="relative aspect-[4/3] bg-[linear-gradient(180deg,#ffffff_0%,#f4f7fb_60%,#ffffff_100%)]">
    <div className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_50%_18%,rgba(255,255,255,0.94),rgba(255,255,255,0)_46%)]" />

    {/* 로고 워터마크 */}
    <Image
      src="/ys-logo-bg.png"
      alt=""
      aria-hidden="true"
      width={1551}
      height={1545}
      className="pointer-events-none absolute left-1/2 top-[2%] h-[90%] w-auto -translate-x-1/2 select-none object-contain opacity-[0.04] brightness-0"
    />

    {section.directors.map((director) => (
      <div key={director.name} className={`absolute -translate-x-1/2 ${director.figureClassName}`}>
        <Image
          src={director.image}
          alt={director.alt}
          fill
          sizes="(min-width: 1024px) 22vw, 44vw"
          className={`object-contain object-top drop-shadow-[0_22px_30px_rgba(15,29,54,0.18)] ${director.imageClassName ?? ''}`}
        />
      </div>
    ))}

    {/* 하단 화이트 페이드 */}
    <div className="pointer-events-none absolute inset-x-0 bottom-0 z-30 h-[26%] bg-[linear-gradient(180deg,rgba(255,255,255,0)_0%,rgba(255,255,255,0.9)_58%,#ffffff_100%)]" />

    {/* 손글씨 이름 라벨 */}
    <div className="pointer-events-none absolute inset-0 z-40 font-handwriting font-normal tracking-[-0.01em] text-navy-900/90">
      {section.directors.map((director) => (
        <span
          key={`${director.name}-label`}
          className={`absolute bottom-[6%] -translate-x-1/2 whitespace-nowrap text-[22px] drop-shadow-[0_2px_8px_rgba(255,255,255,0.92)] md:text-[30px] ${director.labelClassName}`}
        >
          {director.name}
        </span>
      ))}
    </div>
  </div>
) : section.image ? (
  /* 기존 단일 이미지 + navy 그라데이션 오버레이 (병원 소개 섹션용) */
) : null}
```
포인트: h-[145%] + top-[5%] + 컨테이너 overflow-hidden으로 상반신 크롭. 145%는 "머리~팔짱/허리"가 보이는 값 (원래 전신 97% → 상반신 180% → 20% 축소 145%).

## 2. `src/components/ReviewsShowcaseSection.tsx` — 신규 파일 (전체 코드)

홈 치료체험 후기 섹션. 아래 코드 전체를 그대로 복원하면 됨.

```tsx
'use client';

import { useEffect, useRef, useState } from 'react';
import Link from 'next/link';
import { ArrowRight } from 'lucide-react';

type ReviewTone = 'mist' | 'amber' | 'paper' | 'navy';

interface ReviewItem {
  category: string;
  title: string;
  date: string;
}

// TODO: 치료후기 게시판(/board/reviews) 데이터가 쌓이면 실제 데이터 연동으로 교체
// 개수는 톤(4색)·지그재그(2단) 순환이 이어지도록 4의 배수로 유지
const DUMMY_REVIEWS: ReviewItem[] = [
  { category: '무릎', title: '무리해서 뛰다가 무릎 붓고 통증으로 한 달 이상 참았어요.', date: '2025.09.01' },
  { category: '어깨', title: '지난 10월부터 다니는 병원입니다. 원장님이 너무 친절하시고 잘 치료해주셔요.', date: '2025.09.01' },
  { category: '허리', title: '처음 방문했습니다. 다른 병원보다 상세하게 진료봐주시네요.', date: '2025.08.06' },
  { category: '손발', title: '잦은 발목 부상으로 찾게 되었어요! 꼼꼼한 진료 감사합니다.', date: '2025.08.04' },
  { category: '무릎', title: '계단 오를 때마다 시큰했던 무릎, 주사치료 후 등산도 다녀왔습니다.', date: '2025.07.22' },
  { category: '허리', title: '척추관협착증으로 걷기 힘들었는데 내시경 치료 후 산책이 즐거워요.', date: '2025.06.30' },
  { category: '어깨', title: '오십견으로 밤잠을 설쳤는데 충격파 치료받고 팔이 올라갑니다.', date: '2025.06.12' },
  { category: '목', title: '목디스크 초기 진단, 과잉 진료 없이 필요한 치료만 해주셔서 믿음이 가요.', date: '2025.05.28' },
  { category: '손발', title: '빙판이 생긴 곳을 물건 안고 걷다가 넘어져서 팔쪽 근육을 다쳤어요.', date: '2025.04.17' },
  { category: '목', title: '평소 자세도 안 좋고 일을 많이 하다 보니 목어깨 통증이 심해졌어요.', date: '2025.04.17' },
  { category: '도수재활', title: '야간 진료 가능해서 너무 좋아요. 꾸준히 다니면서 몸이 달라졌어요.', date: '2025.04.15' },
  { category: '허리', title: '허리디스크 진단받고 비수술 치료로 3주 만에 일상으로 복귀했습니다.', date: '2025.03.19' },
];

const TONE_STYLES: Record<ReviewTone, { card: string; chip: string; title: string; date: string }> = {
  mist: {
    card: 'bg-[#dfe5f2]/80 border border-white/60 backdrop-blur-md shadow-[inset_0_1px_0_rgba(255,255,255,0.7),0_22px_48px_-32px_rgba(15,29,54,0.45)]',
    chip: 'bg-white text-ink',
    title: 'text-ink',
    date: 'text-ink-sub/70',
  },
  amber: {
    card: 'bg-[#e9ac3e]/85 border border-white/35 backdrop-blur-md shadow-[inset_0_1px_0_rgba(255,255,255,0.45),0_22px_48px_-32px_rgba(122,78,10,0.45)]',
    chip: 'bg-white text-ink',
    title: 'text-navy-950',
    date: 'text-navy-950/70',
  },
  paper: {
    card: 'bg-[#e9eaec]/80 border border-white/60 backdrop-blur-md shadow-[inset_0_1px_0_rgba(255,255,255,0.75),0_22px_48px_-32px_rgba(15,29,54,0.4)]',
    chip: 'bg-white text-ink',
    title: 'text-ink',
    date: 'text-ink-sub/70',
  },
  navy: {
    card: 'bg-navy-800/90 border border-white/10 backdrop-blur-md shadow-[inset_0_1px_0_rgba(255,255,255,0.14),0_22px_48px_-32px_rgba(10,20,40,0.65)]',
    chip: 'bg-white text-navy-900',
    title: 'text-white',
    date: 'text-white/70',
  },
};

const TONE_CYCLE: ReviewTone[] = ['mist', 'amber', 'paper', 'navy'];

const STEP_INTERVAL_MS = 3600;
const SLIDE_DURATION_MS = 1200;
const SLIDE_EASING = 'cubic-bezier(0.45, 0.05, 0.15, 1)';
const LOWER_OFFSET_PX = 60;

const ReviewsShowcaseSection = () => {
  const total = DUMMY_REVIEWS.length;
  const trackRef = useRef<HTMLDivElement>(null);
  const [step, setStep] = useState(0);
  const [animated, setAnimated] = useState(true);
  const [isPaused, setIsPaused] = useState(false);
  const [slotWidth, setSlotWidth] = useState(334);

  useEffect(() => {
    const measure = () => {
      const track = trackRef.current;
      if (!track || track.children.length < 2) return;
      const first = track.children[0] as HTMLElement;
      const second = track.children[1] as HTMLElement;
      setSlotWidth(second.offsetLeft - first.offsetLeft);
    };
    measure();
    window.addEventListener('resize', measure);
    return () => window.removeEventListener('resize', measure);
  }, []);

  useEffect(() => {
    if (isPaused) return;
    const id = setInterval(() => setStep((current) => current + 1), STEP_INTERVAL_MS);
    return () => clearInterval(id);
  }, [isPaused]);

  // 한 바퀴를 다 돌면 슬라이드가 끝난 뒤 애니메이션 없이 처음 위치로 되감기
  useEffect(() => {
    if (step < total) return;
    const timer = setTimeout(() => {
      setAnimated(false);
      setStep((current) => current - total);
    }, SLIDE_DURATION_MS);
    return () => clearTimeout(timer);
  }, [step, total]);

  useEffect(() => {
    if (animated) return;
    const raf = requestAnimationFrame(() => requestAnimationFrame(() => setAnimated(true)));
    return () => cancelAnimationFrame(raf);
  }, [animated]);

  const cards = [...DUMMY_REVIEWS, ...DUMMY_REVIEWS];
  const cardTransition = animated
    ? `transform ${SLIDE_DURATION_MS}ms ${SLIDE_EASING}`
    : 'none';

  return (
    <section className="overflow-hidden bg-[#edf1f8] py-24 md:py-32">
      <div className="mx-auto w-full max-w-[1540px] px-7 xl:px-10">
        <div className="grid grid-cols-1 items-start gap-10 lg:grid-cols-[minmax(0,1fr)_auto]">
          <div className="space-y-5">
            <h2 className="text-4xl font-extrabold leading-[1.14] tracking-tight text-ink md:text-5xl">
              치료체험 후기
            </h2>
            <p className="text-lg font-medium leading-relaxed text-ink-sub md:text-xl">
              연세척에서 치료받으신 환자분들이 직접 남겨주신 후기입니다.
            </p>
            <p className="inline-flex items-center rounded-lg bg-primary-light/70 px-3.5 py-2 text-[13px] font-semibold text-primary">
              ※ 의료법 규정에 따라 자세한 내용은 로그인 후 확인할 수 있습니다.
            </p>
          </div>

          <Link
            href="/board/reviews"
            className="group inline-flex w-fit items-center gap-3 rounded-full border border-ink/25 bg-transparent px-8 py-4 text-[17px] font-bold text-ink transition-all duration-300 hover:border-primary hover:bg-primary hover:text-white lg:mt-2"
          >
            자세히보기
            <ArrowRight size={18} className="transition-transform duration-300 group-hover:translate-x-1" />
          </Link>
        </div>
      </div>

      <div
        className="mt-12 overflow-hidden md:mt-14"
        onMouseEnter={() => setIsPaused(true)}
        onMouseLeave={() => setIsPaused(false)}
      >
        <div
          ref={trackRef}
          className="-ml-32 flex h-[390px] w-max items-start gap-6 md:-ml-40"
          style={{
            transform: `translateX(${-step * slotWidth}px)`,
            transition: cardTransition,
          }}
        >
          {cards.map((review, index) => {
            const tone = TONE_STYLES[TONE_CYCLE[index % TONE_CYCLE.length]];
            const isLower = (((index - step) % 2) + 2) % 2 === 0;

            return (
              <article
                key={`${review.title}-${index}`}
                aria-hidden={index >= total}
                className={`flex h-[330px] w-[280px] shrink-0 flex-col rounded-[26px] p-7 md:w-[310px] ${tone.card}`}
                style={{
                  transform: `translateY(${isLower ? LOWER_OFFSET_PX : 0}px)`,
                  transition: cardTransition,
                }}
              >
                <span className={`inline-flex w-fit items-center rounded-full px-4 py-2 text-[14px] font-extrabold leading-none ${tone.chip}`}>
                  {review.category}
                </span>
                <p className={`mt-5 line-clamp-3 break-keep text-[19px] font-black leading-[1.55] tracking-tight ${tone.title}`}>
                  {review.title}
                </p>
                <span className={`mt-auto pt-8 text-[15px] font-bold tracking-tight ${tone.date}`}>
                  {review.date}
                </span>
              </article>
            );
          })}
        </div>
      </div>
    </section>
  );
};

export default ReviewsShowcaseSection;
```

## 3. `src/app/(public)/page.tsx` — 홈

1. import 추가: `import ReviewsShowcaseSection from '@/components/ReviewsShowcaseSection';`
   (사용자 작업: `import HomeDoctorsRevealSection from '@/components/HomeDoctorsRevealSection';` 도 있었음)
2. `<YoutubeSection />` 다음에 `<ReviewsShowcaseSection />` 삽입.
3. **섹션 3개 삭제**: 
   - `#expertise` "정확한 원칙 정교한 치료" (Specialized Care, 3클리닉 카드)
   - "최상의 결과를 위한 연세척의 3대 원칙" (navy-950 배경)
   - `#reservation-cta` "통증 없는 내일, 함께 시작하세요." (파란 CTA)
   - 삭제로 안 쓰게 된 lucide 아이콘 임포트 제거: Activity, Award, CheckCircle, Shield
4. specializedPrograms 카드(양방향 척추내시경/무릎관절 내시경) 등장 애니메이션 수정:
   ```tsx
   initial={{ opacity: 0, y: 44 }}
   whileInView={{ opacity: 1, y: 0 }}
   whileHover={{ y: -6, transition: { duration: 0.35, ease: 'easeOut' } }}
   viewport={{ once: true, amount: 0.15 }}
   transition={{ delay: index * 0.15, duration: 0.9, ease: [0.22, 1, 0.36, 1] }}
   // className에서 hover:-translate-y-1 제거, transition-all → transition-[background-color,border-color,box-shadow]
   ```
5. Daily Care 섹션 ("모두의 일상이 흔들림 없이…"):
   - 헤더 가운데 정렬: 래퍼 `<div className="mx-auto max-w-4xl text-center">`, 설명 p에 `mx-auto` 추가
   - 캐러셀 그리드: `lg:grid-cols-[auto_minmax(0,560px)] lg:items-center lg:justify-center lg:gap-12 xl:gap-16`
     (이미지 칼럼 auto로 죽은 공간 제거 + 전체 중앙 정렬)
   - (사용자 작업) activeCareIndex 초깃값 1, 5초 자동 넘김 useEffect([activeCareIndex] 의존) 존재했음

## 4. `src/components/Header.tsx` — 척추센터 메가메뉴

- 설명 한 줄로 축약 + 순서 변경 (수술 치료가 비수술치료보다 먼저):
```tsx
{ name: '목디스크', desc: '목·어깨 통증과 팔 저림의 원인을 정밀하게 확인합니다', href: '/treatments/spine/neck-disc' },
{ name: '허리디스크', desc: '허리 통증과 다리 저림·당김 증상을 맞춤 진단합니다', href: '/treatments/spine/disc' },
{ name: '수술 치료', desc: 'UBE 양방향 척추내시경 중심의 최소침습 치료입니다', href: '/treatments/spine/ube' },
{ name: '비수술치료', desc: '주사·시술·재활로 수술 부담을 낮추는 치료입니다', href: '/treatments/spine/non-surgical' },
{ name: '도수 재활 클리닉', desc: '도수·물리·자세교정으로 회복과 재발을 막습니다', href: '/treatments/spine/rehab' },
```

## 5. `src/app/(public)/treatments/spine/disc/page.tsx` — 허리디스크

- "주요 원인" 체크 아이콘 노란색 → 파란색 (목디스크 형식과 동일):
```
bg-[#F7B500] ... rgba(247,181,0,0.9)  →  bg-primary ... shadow-[0_10px_22px_-12px_rgba(40,74,165,0.9)]
```

## 6. `src/components/LumbarDiseaseSection.tsx` — 허리 질환 다이어그램

- 느낌표 마커를 척추 정렬로 이동 (49% → 50.5%), 점선 보정:
```tsx
compression-fracture: point left 50.5%, line { left: '50.5%', width: '15.5%' }, button 66%
scoliosis:            point left 50.5%, line { left: '32%',   width: '18.5%' }, button 22%
stenosis:             point left 50.5%, line { left: '50.5%', width: '15.5%' }, button 66%
lumbar-disc:          point left 50.5%, line { left: '33%',   width: '17.5%' }, button 22%
```
(top 값은 기존 그대로: 38% / 50% / 59% / 69%)

## 7. `src/components/QuickMenu.tsx` — 플로팅 퀵메뉴

- "진료안내" → **"진료일정"**, 아이콘 `ClipboardPlus` → `CalendarDays`, 링크 `/#expertise` → `/doctors#doctors-lineup`

## 8. `src/app/(public)/doctors/page.tsx` — 의료진 페이지

- 라인업(단체 사진) 섹션에 앵커 추가:
```tsx
<section id="doctors-lineup" className="mx-auto max-w-7xl scroll-mt-24 px-6 py-20 md:py-28">
```

---

### 참고 자산 경로 (public/)
- 병원장 누끼: `/generated/doctors-lineup/lee-nam.png`, `/generated/doctors-lineup/kim-dong-han.png`
- 로고 워터마크: `/ys-logo-bg.png` (1551×1545)
- 치료후기 게시판 카테고리: 목 / 허리 / 무릎 / 어깨 / 손발

### 세션 이전(이 세션 밖) 상태에 대한 주의
- public 폴더의 원본 사진들(김동한병원장.jpg, 이남 병원장.jpg 등)과 generated 이미지들도 분실 — GitHub에 있길 기대.
- `HomeDoctorsRevealSection.tsx`는 사용자가 별도로 만든 컴포넌트로 이 기록에 코드 없음.
